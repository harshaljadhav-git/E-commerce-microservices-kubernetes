# k8s/base/secrets/database-secrets.yaml
# WARN: In production, do NOT commit actual secrets to Git.
# Use External Secrets Operator or Sealed Secrets.
# This is a placeholder structure.
apiVersion: v1
kind: Secret
metadata:
  name: database-secrets
  namespace: ecommerce
type: Opaque
stringData:
  # These should be replaced by actual base64 encoded values or injected via CSI Driver
  mysql-username: "root"
  mysql-password: "password"
  mongo-username: "admin"
  mongo-password: "password"
  jwt-secret: "verysecretkeyforjwtsigning_must_be_long_enough_256_bits"
